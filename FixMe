->utils_git (il file è stato inglobato in berrysizeestimator.py. Il file è ancora presente per sicurezza)

1)# from PIL import Image as im # FIXME: questo rename renderà il codice meno chiaro

-l'import è sata rimossa in quanto non necessaria

2)# FIXME aggiungi molte dipendenze in questo progetto, sei sicuro che con cv2,
#   scipy-image, PIL o imageio non puoi fare le stesse cose?

-Credo lei si riferisca a imutils. Questo non è altro che un wrapper di open cv che semplifica molto la parte di ricerca e gestione dei contour 

3)# FIXME: Dovresti dare a tutto il progetto un'approccio MVC (Model Viewer Controller), cioè separare le parti di
#  visualizzazione da quelle che fanno i conti e da quelle che riguardano la configurazione:
#  https://it.wikipedia.org/wiki/Model-view-controller

-Spero di essere riuscito a cogliere la richiesta con le varie "Draw" function

4) #FIXME: aggiungi un modo per interrompere il programma dando comandi da tastiera o quando si
        # chiude la finestra
        
essendo un flow unico, il programma non richiede più l'interazione dell'utente

->measurement_git.py

1-4)#FIXME: incapsula questa scelta di bkg removal in una classe o funzione a parte e rendila
# modificabile usando lo "strategy" pattern (se hai nozioni di ingegneria del software)

# FIXME: a me da errore quando cerca di scaricare UNET perché è un file drive che è stato
        #  scaricato troppe volte. Conviene, se vuoi usare questo modulo, includere i pesi in una cartella locale. È
        #  poco carino che un progetto scrichi la roba i una cartella nascosta nella home...

-Non ho avuto la possibilità di cercare un'alternativa, le lascio il github dove l'ho preso https://github.com/danielgatis/rembg , può comunque commentare le poche righe in cui compare. i risultati saranno un po' peggiori, ma almeno bypassa il problema

2-3)#FIXME: le funzionalità di questo main dovrebbero essere incapsulate in una classe, così che il risultato finale sia
# un codice in cui la classe può essere importata in altri progetti. Il main lo usi come uno script di data,
# o meglio ancora, ti fai una struttura di data automatico con pytest.

# FIXME: rimuovi reference a path assoluti, crea una variabile ROOT_DIR in un file di configurazione python e usa
    #  il comando os.path.realpath(os.path.join(os.path.dirname(__file__), '..')) per impostarla. Tutti i path che
    #  usi nel progetto devono essere riferiti a questa
    
Non sono riuscito ad apportare questi cambiamenti perché non ho fatto in tempo, ci lavorerò

5) # FIXME: non sono sicuro che sia RGB, penso sia BGR di base. Attento anche sopra con il bkg removal. Fai un
        #  check
        
-Fixato


